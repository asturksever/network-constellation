## What this changes

<!-- One or two sentences, and why. -->

## Checked

- [ ] `npm run check && npm test` pass
- [ ] `npm run bundle:demo` builds (it catches two modules declaring the same top-level name)
- [ ] Looked at it in a browser — `npm run pages` shows it as the public site will
- [ ] No real export, name, headline, logo or key anywhere in the diff, screenshots included (the demo's invented people only)
- [ ] If this sends data anywhere new: the README's Privacy section and the Content-Security-Policy in `index.html` say so in this same change
